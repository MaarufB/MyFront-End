// ARROW FUNCTIONS THE BASICS

let sum = (a, b) => a + b;
let sumResult = sum(2,2);

console.log(sumResult);

let product = (a, b) =>{
    const first = a;
    const second = b;
    return first * second;
}

let resultProduct = product(2,2);
console.log(resultProduct);